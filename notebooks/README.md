# Notebooks

`ffn_permutation_core.ipynb` 是本仓库的主阅读入口，已执行并包含输出。它从 `experiments/` 下的机器可读结果重建全部图表，不加载模型，也不需要 GPU。

`build_core_notebook.py` 保存 Notebook 的源叙事与代码单元。修改后先运行构建脚本，再执行 Notebook；最终提交应同时包含构建脚本与带输出的 Notebook。
